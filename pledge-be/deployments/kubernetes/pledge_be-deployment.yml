apiVersion: apps/v1
kind: Deployment
metadata:
  name: pledge-be-dm
  namespace: pledge-be
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pledge-be
  template:
    metadata:
      name: pledge-be-pod
      labels:
        app: pledge-be
    spec:
      containers:
        - name: pledge-be
          image: /pledge-be/pledge-be:latest
          # If using a local image, use Never, default is Always
          #imagePullPolicy: Never
          command: ["./pledge_be", "-c", "/app/configs/pledge_be.yml"]
          resources:
            requests:
              cpu: 10m
              memory: 10Mi
            limits:
              cpu: 1000m
              memory: 1000Mi
          volumeMounts:
            - name: pledge-be-vl
              mountPath: /app/configs/
              readOnly: true
            - name: host-timezone
              mountPath: /etc/localtime
              readOnly: true
          #env:
          #  - name: TZ
          #    value: "Asia/Shanghai"
          
          ports:
            - name: http-port
              containerPort: 8080
          readinessProbe:
            httpGet:
              port: http-port
              path: /health
            initialDelaySeconds: 10
            timeoutSeconds: 2
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              port: http-port
              path: /health

            initialDelaySeconds: 10
            timeoutSeconds: 2
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
      # todo for private repositories, you need to create a secret (here docker-auth-secret) to store the account and password to log into docker
      imagePullSecrets:
        - name: docker-auth-secret
      volumes:
        - name: pledge-be-vl
          configMap:
            name: pledge-be-config
        - name: host-timezone
          hostPath:
            path: /etc/localtime
            type: File
